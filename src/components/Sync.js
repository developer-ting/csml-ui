// MODULES //

// COMPONENTS //

// SECTIONS //

// PLUGINS //

// STYLES //
import styles from "../styles/components/sync.module.scss";

// IMAGES //

/** Home Hero Section */
const Sync = () => {
	return (
		<section className={`${styles.sync} ptb_100`}>
			<div className="sync_info">
				<h2 className="heading_text_55 pb_50 text_center toTop" data-scroll>
					SYNC Scoring System – The world’s <br />
					fastest-selling bowling scoring system
				</h2>
			</div>
		</section>
	);
};

export default Sync;
